#name: CI
#
#on:
#  push:
#    branches: [ main, develop ]
#  pull_request:
#
#jobs:
#  python:
#    runs-on: ubuntu-latest
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#
#      - name: Set up Python 3.11
#        uses: actions/setup-python@v5
#        with:
#          python-version: '3.11'
#
#      - name: Install dependencies
#        run: |
#          python -m pip install --upgrade pip
#          pip install -r pc_controller/requirements.txt
#          pip install pytest ruff mypy pytest-cov
#
#      - name: Ruff (lint)
#        run: ruff check .
#
#      - name: Mypy (type check)
#        run: mypy
#
#      - name: Run pytest with coverage (Python Hub)
#        env:
#          QT_QPA_PLATFORM: offscreen
#          OPENCV_LOG_LEVEL: ERROR
#        run: pytest -vv -ra -s --cov=pc_controller/src --cov-report=term-missing --cov-report=xml
#
#      - name: Upload Python coverage artifact
#        uses: actions/upload-artifact@v4
#        with:
#          name: python-coverage
#          path: coverage.xml
#
#  android:
#    runs-on: ubuntu-latest
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#
#      - name: Set up JDK 17
#        uses: actions/setup-java@v4
#        with:
#          distribution: 'temurin'
#          java-version: '17'
#
#      - name: Gradle wrapper validation
#        uses: gradle/wrapper-validation-action@v2
#
#      - name: Gradle build cache
#        uses: gradle/gradle-build-action@v3
#        with:
#          cache-read-only: false
#
#      - name: Run Android unit tests (Robolectric/JVM)
#        run: |
#          ./gradlew --no-daemon --console=plain :android_sensor_node:tests --stacktrace || true
#          ./gradlew --no-daemon --console=plain :android_sensor_node:app:testDebugUnitTest --stacktrace
#
#      - name: Upload Android unit test reports (HTML)
#        if: always()
#        uses: actions/upload-artifact@v4
#        with:
#          name: android-test-report-html
#          path: android_sensor_node/app/build/reports/tests/testDebugUnitTest
#          if-no-files-found: ignore
#
#      - name: Upload Android unit test results (XML)
#        if: always()
#        uses: actions/upload-artifact@v4
#        with:
#          name: android-test-results-xml
#          path: android_sensor_node/app/build/test-results/testDebugUnitTest
#          if-no-files-found: ignore
