package com.yourcompany.sensorspoke.ui

import androidx.lifecycle.ViewModel

/**
 * MainViewModel holds UI state for MainActivity. Phase 1 has no UI state,
 * but we keep this scaffold to follow MVVM architecture for future phases.
 */
class MainViewModel : ViewModel()
